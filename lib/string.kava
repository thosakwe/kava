class String(len: uint64, data: uint8*) : Object {
  static from (data: uint8[]) -> this(data.length, data.pointer)

  operator +(other: any) {
    return match other with
      | String(l, d) {
          // TODO: Implement this
        }
      // | int -> this + Int(other)
      | Object -> this + other.toString()
  }
}
